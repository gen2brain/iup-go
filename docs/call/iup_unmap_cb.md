## UNMAP_CB

Called right before an element is unmapped in [IupUnmap](../func/iup_unmap.md).

### Callback

    int function(Ihandle *ih);

**ih**: identifier of the element that activated the event.

### Affects

All elements that have a native representation.
